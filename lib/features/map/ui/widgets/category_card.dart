import 'package:flutter/material.dart';

Card buildCategoryCard(thing, BuildContext context) {
  return Card(
      margin: EdgeInsets.fromLTRB(70.0, 14.0, 70.0, 0),
      child: Padding(
          padding: const EdgeInsets.fromLTRB(2.0, 12.0, 12.0, 12.0),
          child: Column(children: <Widget>[
            Container(),
          ])
      )
  );
}